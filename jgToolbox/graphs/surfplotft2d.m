function [outmatrix] = surfplotft2d(inmatrix)% function [outmatrix] = surfplotft2d(inmatrix)%% Plots [inmatrix] and the Fourier transform of [inmatrix],% with 3D surface subplots of the signal, amplitude, and phase.% Used the function FT2D.m.  Returns the FT in [outmatrix].%% April 14, 1997   J Gold  J Hillis  U of Toronto Vision Lab.% Get the Fourier transform.outmatrix = ft2D(inmatrix);% Make a subplot with three graphs.% The signal.subplot(3,1,1);surf(inmatrix);grid on;% The amplitude.subplot(3,1,2);surf(abs(outmatrix));grid on;title('Amplitude');% The phase.subplot(3,1,3);surf(angle(outmatrix));grid on;title('Phase');% END.  