function [outvector] = plotft1d(invector)% function [outvector] = plotft1d(invector)%% Plots [invector] and the Fourier transform of [invector],% with 2D subplots of the signal, amplitude, and phase.% Used the function FT1D.m. Also returns the FT as [outvector].%% April 14, 1997   J Gold  J Hillis  U of Toronto Vision Lab.% Get the Fourier transform.outvector = ft1D(invector);% Get meaningful frequency values for plots.n = length(invector);			% Ther number of points in the vector.if rem(n,2) == 1				% Make the indeces of the coefficients.	k = (-(n-1)/2):(n-1)/2;		% If n is odd, this is k.	else	k = (-n/2):(n/2-1);			% If n is even, this is k.end% Make a subplot with three graphs.% The signal.subplot(3,1,1);plot(invector);grid on;title('Signal');ylabel('Contrast');% The amplitude.subplot(3,1,2);plot(k,abs(outvector),'go');grid on;title('Amplitude');ylabel('Magnitude');% The phase.subplot(3,1,3);plot(k,atan2(imag(outvector),real(outvector)),'bo');grid on;title('Phase');ylabel('Angle');xlabel('Frequency (Cycles Per Vector)');% END.  