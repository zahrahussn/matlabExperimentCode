function [x,y] = getweib(params,limits,rate,polarity)% function [x,y] = getweib(delta,gamma,alpha,beta,limits)%% Returns the x and y values for the parameters of a weibull function.delta = params(1);gamma = params(2);alpha = params(3);beta = params(4);% Defaults for the limits of the sample.if nargin < 2	limits(1) = 0;	limits(2) = 128;endif nargin < 3	rate = 1;endif nargin < 4	polarity = 1; end%weibfit = inline(sprintf('%f+%f*(1-exp(-(x./%f).^%f))',delta,gamma,alpha,beta));%[x,y] = fplot(weibfit,[limits(1) limits(2)],(limits(2)-limits(1)));x = linspace(limits(1),limits(2),abs(limits(2)-limits(1))*rate)';if polarity ~= -1	y = delta+gamma*(1-exp(-(x./alpha).^beta));else	y = delta+gamma.*exp(-(x./alpha).^beta);endreturn;