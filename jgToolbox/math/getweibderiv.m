function [x,y] = getweibderiv(params,limits,rate,polarity)% function [x,y] = getweibderiv(gamma,alpha,beta,limits)%% Returns the x and y values for the parameters of the first % derivative of the weibull function. % Defaults for the limits of the sample.if nargin < 2	limits(1) = 0;	limits(2) = 128;	rate = 1;endif nargin < 3	rate = 1; endif nargin < 4	polarity = 1; enddelta = params(1);gamma = params(2);alpha = params(3);beta = params(4);%weibderivfit = inline(sprintf('%f*exp(-(x./%f).^%f)*%f/%f*(x./%f).^(%f-1)',gamma,alpha,beta,beta,alpha,alpha,beta));%[x,y] = fplot(weibderivfit,[limits(1) limits(2)],(limits(2)-limits(1)));x = linspace(limits(1),limits(2),abs(limits(2)-limits(1))*rate)';y = polarity.*gamma.*(exp(-(x./alpha).^beta))*(beta/alpha).*(x./alpha).^(beta-1);return;