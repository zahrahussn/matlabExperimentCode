function [x,y] = getnormpdf(params,limits,rate,polarity)% function [x,y] = getnormpdf(gamma,mu,sigma,limits,rate,polarity)%% Returns the x and y values for the parameters of the first % derivative of the cumulative normal function. delta = params(1);gamma = params(2);mu = params(3);sigma = params(4);% Defaults for the limits of the sample.if nargin < 2	limits(1) = 0;	limits(2) = 128;endif nargin < 3	rate = 1; endif nargin < 4	polarity = 1; endx = linspace(limits(1),limits(2),abs(limits(2)-limits(1))*rate)';y = polarity.*gamma.*normpdf(x,mu,sigma);return;