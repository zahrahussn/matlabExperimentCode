function dist = dogauss(sz,sigma,mu)% function dist = dogauss(sz,sigma,mu)%% creates a 1d Gaussian distribution matrix% % parameters:% sz 	-	the length of the vector, in pixels% sigma	-	the standard deviation% mu 	-	the mean; default is zero%% the final vector is normalized to a height of 1% dist = -1 if not enough arguments are passed%% July 16, 1998  JMG U of Toronto Vision Lab% default for meanif nargin < 3	mu=0;endif nargin>1	x = (-sz/2:sz/2-1);	dist = (1/(sqrt(2*pi*sigma^2)))*(exp(-((x/sigma).^2/2)));	dist=dist/max(max(dist));else	printstr('Not enough parameters.');	dist=-1;end	return