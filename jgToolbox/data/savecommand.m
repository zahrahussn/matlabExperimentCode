function savecommand(fid,str)% function savecommand(fid,str)%% saves an executable command or series of commands to a data file.% commands should be sent as one continuous string.% appends a semicolon to the end of the command string if one is not present.%% July 1 1998  JMG U of T Vision Labif str(end) ~= ';'	str = [str,';'];endstr = ['%% *',str,'\n'];fprintf(fid,str);return