function alias(setting)% function alias(setting)% % uses applescript to turn font anti-aliasing on or off.% this allows user to prevent text from appearing unreadable in % experiemental windows.%% setting = 'on' to turn anti-aliasing on% setting = 'off' to turn anti-aliasing off% if setting is anything else, it does nothing%% March 2, 2000  JMG U of T Vision Labdefarg('setting','on');setting = lower(setting);if strcmp(setting,'on') | strcmp(setting,'off')	resultString = applescript(['alias',setting,'.as']);	resultString = resultString(2:end-1); % strip off excess quotes	disp(resultString);endreturn