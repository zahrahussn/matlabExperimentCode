function s = inregion(xloc,regionloc,sz)% function s = inregion(x,image,sz)%% Returns 1 if xloc(v,h) is within a szxsz square with top left% coordinate regionloc(v,h). Returns 0 if not.%% June 22, 1996		J Gold	University of Toronto Vision Lab   % July 1998:  		JMG cleaned it up.cv=xloc(1);				% The selected coordinates' vertical coordinate.ch=xloc(2);				% The selected coordinates' horizontal coordinate.iv=regionloc(1);			% The image's upper lefthand coordinate.ih=regionloc(2);			% The image's upper lefthand coordinate.s=0if ((cv >= iv) & (cv <= iv+sz(1)) & (ch >= ih) & (ch <= ih+sz(2)))	s = 1; endreturn