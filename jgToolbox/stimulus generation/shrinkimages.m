function [s] = shrinkimages(s,k)% function [m] = shrinkimages(s,k)%% resize a set of images in a struct array% s is the struct of images% k is the shrink factor (see imresize.m).% DEPENENCIES: 'imresize.m' from matlab image toolbox%% March 3  1999  JMGif nargin > 1	names = fieldnames(s);	n=length(names);	for i = 1:n		eval(['s.',names{i},' = imresize(s.',names{i},',k);']);	endendreturn;