function [density,pixarea] = nsd(variance,screensz,res,vd)% function [density,pixarea] = nsd(variance,screensz,res,vd)%% computes the spectral density of a gaussian noise of a given% contrast variance and size. the result is expressed in degrees^2% of visual angle.%% NB: this is not the same as noise spectral density in terms of% cycles/image.%% parameters:%% variance-   the contrast variance of the noise field% screensz-   a 2 element vector containing the width and %			  height of the screen, respectievely, in cm. %			  (i.e., [width height]).% res-		  a 2 element vector containing the width and %			  height of the screen, respectievely, in pixels. %			  (i.e., [width height]).% vd-		  the viewing distance, in cm.%% output:%% density-    the spectral density of the noise, in degrees^2.% pixarea-    the area of a single pixel, in degrees^2.if nargin == 4		pixarea = (visang(vd,pix2cm(1,screensz,res)))^2;	density = variance/(1/pixarea);else	error('not enough parameters sent.');endreturn;