function str = vars2str(varargin)% converts a set of variables to a string% of variables which can be sent to the % eval function. numbers and strings are% both accepted.%% 9/29/99 JMG U of T vision labif length(varargin)			str = '';		for i = 1:length(varargin)		if isstr(varargin{i})			str = [str,inputname(i),' = ',quotestring(varargin{i}),';'];		else			str = [str,inputname(i),' = [',num2str(varargin{i}),'];'];		end	endendreturn