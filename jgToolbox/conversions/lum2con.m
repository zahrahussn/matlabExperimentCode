function [OutMatrix,Lave] = Lum2Con(InMatrix)% function [OutMatrix,Lave] = Lum2Con(InMatrix)%% This function converts an square input matrix into % contrast values (instead of luminance) and returns% it in OUTMATRIX. Contrast is defined here as%%  		(L(pixel)-Laverage)/Laverage%% Also, the average luminance is returned in LAVE% for future reference.%% June 22, 1996		J Gold	P J Bennett A B Sekuler 	University of Toronto Vision Lab   Lave = mean(InMatrix(:));OutMatrix = InMatrix - Lave;		OutMatrix = OutMatrix./Lave;	 	return	