function [nz,rs]=noisewave(nc,cvar,exact);%%	function [nz,rs]=noisewave(nr,cvar,exact);%	nc == # of columns%	cvar == contrast variance%	exact == when ~= 0, function returns a wave with a variance that is EXACTLY cvar, otherwise%			 the variance is cvar ON AVERAGE.%	returns nz, which is a 1 x nc row vector; and rs, which is the seed for the random number generator%if (nargin<3)	exact=0;end;rs=randn('state');temp=randn(1,nc);if (exact~=0)	tv=var(temp);else	tv=1;end;nz=temp*sqrt(cvar/tv);	% nz now has a variance of cvarreturn;