function datarec=psyfuncInit(rectype,initdata)%% function datarec=psyfuncInit(rectype,initdata)% rectype = 'staircase' or 'constantstimulus'% initdata:% if rectype == staircase, then initdata is the following structure:%	initdata.down: # of correct responses need to make staircase go down%	initdata.values: an array of possible values of the independent variable%	initdata.startvalue: starting value of the staircase%	initdata.maxtrials: maximum number of trials that a staircase can run%	initdata.maxrevs: max reversals that a staircase can run% N.B. a staircase is terminated when EITHER maxtrials or maxrevs is exceeded, whichever comes first...% if rectype == constantstimulsu, then initdata is the following structure%	initdata.values: an array of possible values for the independent variable%	initdata.trialspervalue: the number of trials each value is presented%	switch lower(rectype)	case 'staircase'		datarec=scInit(initdata.down,initdata.values,initdata.startvalue,initdata.maxtrials,initdata.maxrevs);	case 'constantstimulus'		datarec=constimInit(initdata.values,initdata.trialspervalue);	otherwise		disp('ERROR in psyfuncInit: Unknown type of data record.');		beep;	end;return;