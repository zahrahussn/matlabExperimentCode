function newrect = centrerect( outer, inner, roundflag )% CENTRERECT  Centre one rectangle in another%% newrect = centrerect( outer, inner, roundflag )defarg('roundflag',1);innersize = rectsize(inner);outersize = rectsize(outer);if roundflag,	offset = round( [ (outersize(1)-innersize(1))/2 (outersize(2)-innersize(2))/2 ] );else	offset = [ (outersize(1)-innersize(1))/2 (outersize(2)-innersize(2))/2 ];endnewrect = [ outer(1:2)+offset outer(1:2)+offset+innersize ];return