function rndmat = rnginit( nvect, dim )% RNGINIT  Generate matrix of uniformly distributed initial positions.%% rndmat = rnginit( nvect, dim )%     - <nvect> is number of positions to generate%     - <dim> is [ maxX maxY ], which defines the range of positions 1:maxX, 1:maxYmaxX=dim(1);maxY=dim(2);rndmat = [ floor(maxX*rand(nvect,1)), floor(maxY*rand(nvect,1)) ] + 1;return