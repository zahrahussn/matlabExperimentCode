function newdata = pooldata( data, condition )% POOLDATA  Pool data across sessions%% newdata = pooldata( data, condition )% 17-Jan-00 -- created (RFM)defarg('condition','');if ~isempty(condition),	data=culldata(data,condition);endnewdata{1}.lines=[];for i=1:size(data,2),	newdata{1}.lines=[ newdata{1}.lines ; data{i}.lines ];	newdata{1}.cmd=data{i}.cmd;endreturn